# E11 optional CGAL tool license

`convex_envelope.cpp` is an isolated optional command-line tool licensed under
GPL-3.0-or-later because it uses CGAL's GPL-covered convex-hull package. It is
not linked into the Apache-2.0 FrayID Python package. The tool consumes and
emits strict text interchange files only.
